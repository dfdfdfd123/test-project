<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bitc.fullstack503.server.mapper.BranchMapper">

    <!-- 대리점 주문 목록 조회 (필터링 추가) -->
    <select id="selectOrdersByBranchWithFilters" resultType="bitc.fullstack503.server.dto.OrderDTO">
        SELECT
        order_id,
        branch_id,
        order_date,
        order_status,
        order_price,
        order_deny
        FROM
        orders
        WHERE
        branch_id = #{branchId}
        <if test="orderId != null and orderId != ''">
            AND order_id = #{orderId}
        </if>
        <if test="startDate != null and startDate != ''">
            AND order_date >= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            <![CDATA[
        AND order_date <= #{endDate}
        ]]>
        </if>
        ORDER BY
        order_date DESC
    </select>

    <!-- 대리점 주문 목록 조회 -->
    <select id="selectOrdersByBranch" resultType="bitc.fullstack503.server.dto.OrderDTO">
        SELECT
        order_id,
        branch_id,
        order_date,
        order_status,
        order_price,
        order_deny
        FROM
        orders
        WHERE
        branch_id = #{branchId}
        ORDER BY
        order_date DESC
    </select>

    <!-- 주문 상세 항목 조회 -->
    <select id="selectOrderItemsByOrderId" resultType="bitc.fullstack503.server.dto.OrderItemDTO">
        SELECT
        order_item_id,
        order_id,
        part_id,
        order_item_quantity,
        order_item_price
        FROM
        order_item
        WHERE
        order_id = #{orderId}
    </select>

    <!-- 주문 상세의 부품 정보 조회 -->
    <select id="selectPartsByOrderId" resultType="bitc.fullstack503.server.dto.PartDTO">
        SELECT
        oi.part_id AS partsId,
        p.part_name AS partName,
        p.part_cate AS partCate,
        oi.order_item_quantity AS quantity,
        oi.order_item_price AS partPrice
        FROM
        order_item oi
        JOIN
        parts p ON oi.part_id = p.part_id
        WHERE
        oi.order_id = #{orderId}
    </select>

</mapper>